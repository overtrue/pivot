---
title: OAuth Flow
description: 用于显示 OAuth 流程的组件。
component: true
---

用于显示 OAuth 流程的组件。

<ComponentPreview name="oauth-flow-demo" />

## 安装

```bash
npx shadcn@latest add https://pivotkit.vercel.app/registry.json oauth-flow
```

## 使用方法

```tsx
import { OAuthFlow } from "@/components/pivot/oauth-flow";
```

```tsx
<OAuthFlow />
```

## 示例

### 基本用法

```tsx
<OAuthFlow />
```

## API 参考

### Props

| 属性        | 类型     | 默认值 | 描述            |
| ----------- | -------- | ------ | --------------- |
| `className` | `string` | -      | 额外的 CSS 类名 |
